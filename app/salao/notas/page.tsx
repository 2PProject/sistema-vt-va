'use client'

import { useCallback, useEffect, useMemo, useRef, useState } from 'react'
import { useRouter } from 'next/navigation'
import LayoutAdmin from '../../../components/LayoutAdmin'
import NotaPreviewDialog from '../../../components/salao/NotaPreviewDialog'
import { supabase, type Empresa } from '../../../lib/supabase'
import { SALAO_COMPETENCIA_INICIAL, SALAO_ENABLED } from '../../../lib/salao/config'
import { listarCertificados, sincronizarNFSe, rotuloAmbiente, type DiagEmpresaSync, type ResumoSync } from '../../../lib/salao/certificados'
import { listarNotas, resumoNotas, type NotaRecebida } from '../../../lib/salao/notas'
import { classificarNota, editarNota, excluirNota, setAnaliseManual, setNotaConferida } from '../../../lib/salao/conferencia'
import { formatarMoeda, MESES } from '../../../utils/calculoVT'

function mesAtual(){const d=new Date();return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}`}
function fimMes(m:string){const[y,mm]=m.split('-').map(Number);return new Date(y,mm,0).toISOString().slice(0,10)}
function mesesEntre(a:string,b:string){const out:string[]=[];let[y,m]=a.split('-').map(Number);const[fy,fm]=b.split('-').map(Number);while(y*12+m<=fy*12+fm&&out.length<120){out.push(`${y}-${String(m).padStart(2,'0')}`);m++;if(m>12){m=1;y++}}return out}
function fmtData(v:string|null){if(!v)return '—';const[a,m,d]=v.split('-');return `${d}/${m}/${a}`}
function fmtDoc(v:string|null){const s=(v||'').replace(/\D/g,'');return s||'—'}
function notaHistorica(n:{data_emissao:string|null}){return !!n.data_emissao&&n.data_emissao<`${new Date().getFullYear()}-01-01`}
type Filtro='todas'|'pendentes'|'conferidas'|'divergentes'|'analise'|'canceladas'
// Detecta cancelamento pelo XML guardado (ABRASF: NfseCancelamento/DataCancelamento;
// nacional: evento tpEvento 1011xx/1051xx; situação=2). Best-effort, limitado a 60k chars.
function notaCanceladaXml(xml?:string|null){if(!xml)return false;const s=xml.length>60000?xml.slice(0,60000):xml;if(/<(?:[\w-]+:)?(NfseCancelamento|NFSeCanc|eCancelamento|DataCancelamento|DataHoraCancelamento|dhCanc)\b/i.test(s))return true;const tp=(s.match(/<(?:[\w-]+:)?(?:tpEvento|cEvento)\b[^>]*>([^<]+)</i)?.[1]||'').replace(/\D/g,'');if(/^(1011|1051|1055)/.test(tp))return true;const sit=s.match(/<(?:[\w-]+:)?(?:situacaoNfse|cSitNFSe)\b[^>]*>([^<]+)</i)?.[1]?.trim();return sit==='2'}

export default function NotasPage(){
 const router=useRouter();const[empresas,setEmpresas]=useState<Empresa[]>([]);const[empresaId,setEmpresaId]=useState('');const[inicio,setInicio]=useState(SALAO_COMPETENCIA_INICIAL);const[fim,setFim]=useState(mesAtual());const[compInicio,setCompInicio]=useState(SALAO_COMPETENCIA_INICIAL);const[compFim,setCompFim]=useState(mesAtual());const[busca,setBusca]=useState('');const[buscaAplicada,setBuscaAplicada]=useState('');const[filtro,setFiltro]=useState<Filtro>('pendentes');const[linhas,setLinhas]=useState<NotaRecebida[]>([]);const[loading,setLoading]=useState(false);const[baixando,setBaixando]=useState(false);const[importando,setImportando]=useState(false);const[reconferindo,setReconferindo]=useState(false);const[reprocCancel,setReprocCancel]=useState(false);const[msg,setMsg]=useState('');const[selecionada,setSelecionada]=useState<NotaRecebida|null>(null);const[editando,setEditando]=useState(false);const[form,setForm]=useState<Record<string,string>>({});const[usuario,setUsuario]=useState<string|undefined>();const requisicao=useRef(0);const pararBaixa=useRef(false);const seletorXml=useRef<HTMLInputElement>(null);const[acaoId,setAcaoId]=useState('');const[syncResumo,setSyncResumo]=useState<ResumoSync|null>(null);const[progressoBaixa,setProgressoBaixa]=useState({ativo:false,percentual:0,etapa:''})
 useEffect(()=>{if(!SALAO_ENABLED){router.replace('/dashboard');return}supabase.from('empresas').select('*').order('razao_social').then(({data})=>setEmpresas(data||[]));supabase.auth.getUser().then(({data})=>setUsuario(data.user?.email))},[router])
 const carregar=useCallback(async()=>{const id=++requisicao.current;setLoading(true);try{const r=await listarNotas({empresaId:empresaId||undefined,de:inicio?`${inicio}-01`:undefined,ate:fim?fimMes(fim):undefined,busca:buscaAplicada,classificacao:'profissional'});if(id===requisicao.current)setLinhas(r)}catch(e){if(id===requisicao.current)setMsg(e instanceof Error?e.message:'Erro ao carregar notas.')}finally{if(id===requisicao.current)setLoading(false)}},[empresaId,inicio,fim,buscaAplicada])
 useEffect(()=>{const t=setTimeout(()=>setBuscaAplicada(busca.trim()),300);return()=>clearTimeout(t)},[busca])
 useEffect(()=>{if(SALAO_ENABLED)carregar()},[carregar])
 const canceladas=useMemo(()=>{const s=new Set<string>();for(const n of linhas)if(notaCanceladaXml(n.xml_original))s.add(n.id);return s},[linhas])
 const visiveis=useMemo(()=>linhas.filter(n=>{const comp=n.competenciaEfetiva||'';if(compInicio&&comp<compInicio)return false;if(compFim&&comp>compFim)return false;if(filtro==='canceladas')return canceladas.has(n.id);if(filtro==='pendentes')return !n.conferida&&!n.analise_manual;if(filtro==='conferidas')return n.conferida;if(filtro==='divergentes')return !!n.competenciaEfetiva&&!!n.data_emissao&&n.competenciaEfetiva!==n.data_emissao.slice(0,7);if(filtro==='analise')return !!n.analise_manual;return true}),[linhas,filtro,compInicio,compFim,canceladas])
 const resumo=resumoNotas(linhas)
 async function baixar(reset=false,alvoId?:string){
  if(baixando)return
  pararBaixa.current=false;setBaixando(true);setMsg('')
  setProgressoBaixa({ativo:true,percentual:0,etapa:'Localizando unidades com certificado…'})
  try{
   const certificados=await listarCertificados()
   const ids=Array.from(new Set(certificados.map(c=>c.empresa_id).filter(id=>alvoId?id===alvoId:(!empresaId||id===empresaId))))
   if(!ids.length)throw new Error(empresaId?'Esta unidade não possui certificado cadastrado.':'Nenhuma unidade possui certificado cadastrado.')
   const nomeEmpresa=(id:string)=>{const e=empresas.find(x=>x.id===id);return e?.apelido||e?.razao_social||'Unidade'}
   // Atualiza UM cartão sem tocar nos demais (workers rodam em paralelo).
   const patchCard=(id:string,patch:Partial<DiagEmpresaSync>)=>setSyncResumo(prev=>prev?{...prev,empresas:(prev.empresas||[]).map(c=>c.empresa_id===id?{...c,...patch}:c)}:prev)
   setSyncResumo({ok:true,ambiente:'gov.br',notasEncontradas:0,registrosAtualizados:0,empresas:ids.map(id=>({empresa_id:id,empresaNome:nomeEmpresa(id),ok:false,status:-1,encontradas:0,gravadas:0,ignoradas:0,houveMais:true,ativo:false,etapa:'Na fila…',rodadas:0}))})
   setProgressoBaixa({ativo:true,percentual:0,etapa:`Consultando ${ids.length} unidade(s) no gov.br…`})
   let totalEncontradas=0,totalGravadas=0,totalIgnoradas=0,totalCanceladas=0,concluidas=0
   const atualizarTopo=()=>setProgressoBaixa({ativo:true,percentual:ids.length?Math.round(concluidas/ids.length*100):0,etapa:`Baixa contínua · ${concluidas}/${ids.length} unidade(s) prontas · ${totalEncontradas} encontrada(s) · ${totalGravadas} gravada(s)`})
   const worker=async(id:string)=>{
    let encontradas=0,gravadas=0,ignoradas=0,canceladas=0,igProprias=0,igSemValor=0,igCancel=0,ultimaDica='',ultimaAmostra=''
    for(let rodada=1;rodada<=800&&!pararBaixa.current;rodada++){
     patchCard(id,{ativo:true,rodadas:rodada,etapa:rodada===1?'Consultando o gov.br…':`Buscando mais (rodada ${rodada})…`})
     const r=await sincronizarNFSe(id,reset&&rodada===1)
     if(r.erro)throw new Error(r.erro)
     const e=r.empresas?.[0]
     if(!e)throw new Error(`${nomeEmpresa(id)} não retornou diagnóstico.`)
     encontradas+=Number(e.encontradas||0);gravadas+=Number(e.gravadas||0);ignoradas+=Number(e.ignoradas||0);canceladas+=Number(e.canceladas||0)
     igProprias+=Number(e.ignoradasProprias||0);igSemValor+=Number(e.ignoradasSemValor||0);igCancel+=Number(e.ignoradasCanceladas||0)
     if(e.dica)ultimaDica=e.dica;if(e.amostra)ultimaAmostra=e.amostra
     totalEncontradas+=Number(e.encontradas||0);totalGravadas+=Number(e.gravadas||0);totalIgnoradas+=Number(e.ignoradas||0);totalCanceladas+=Number(e.canceladas||0)
     const faltam=e.maxNsu&&e.ultimoNsu!=null?Math.max(0,e.maxNsu-e.ultimoNsu):0
     const etapa=e.erro?e.erro
       :e.status===429?'gov.br limitou (429) — aguardando…'
       :e.houveMais?(faltam?`Baixando… faltam ~${faltam} documento(s)`:'Baixando mais lotes…')
       :encontradas?`Concluída · ${gravadas} gravada(s)`:'Em dia — nada novo desde o último NSU'
     patchCard(id,{...e,empresaNome:e.empresaNome||nomeEmpresa(id),encontradas,gravadas,ignoradas,canceladas,ignoradasProprias:igProprias,ignoradasSemValor:igSemValor,ignoradasCanceladas:igCancel,dica:ultimaDica||undefined,amostra:ultimaAmostra||e.amostra,ativo:e.houveMais||e.status===429,etapa,rodadas:rodada})
     setSyncResumo(prev=>prev?{...prev,ambiente:r.ambiente||prev.ambiente,notasEncontradas:totalEncontradas,registrosAtualizados:totalGravadas}:prev)
     if(e.status===429){for(let restante=30;restante>0&&!pararBaixa.current;restante--){patchCard(id,{etapa:`gov.br limitou. Retomando em ${restante}s…`});await new Promise(resolve=>setTimeout(resolve,1000))}continue}
     if(!e.houveMais){patchCard(id,{ativo:false});concluidas++;atualizarTopo();return}
     atualizarTopo()
     await new Promise(resolve=>setTimeout(resolve,1200))
    }
    patchCard(id,{ativo:false})
    if(!pararBaixa.current)throw new Error(`${nomeEmpresa(id)} atingiu o limite de segurança. Clique novamente para continuar do último NSU.`)
   }
   const resultados=await Promise.allSettled(ids.map(worker))
   const falhas=resultados.filter((r):r is PromiseRejectedResult=>r.status==='rejected')
   await carregar()
   if(pararBaixa.current){setProgressoBaixa({ativo:true,percentual:100,etapa:`Parado · ${totalGravadas} nota(s) gravada(s) até aqui`});setMsg('Baixa interrompida pelo usuário. O próximo processamento continuará do último NSU.')}
   else if(falhas.length){const detalhes=falhas.map(f=>f.reason instanceof Error?f.reason.message:String(f.reason)).join(' | ');setProgressoBaixa({ativo:true,percentual:100,etapa:`Finalizado com falha em ${falhas.length} unidade(s)`});setMsg(detalhes)}
   else{setProgressoBaixa({ativo:true,percentual:100,etapa:'Baixa concluída — conferindo automaticamente…'});let autoTxt='';try{const{conferidas,pendentes}=await reconciliarIntervalo(SALAO_COMPETENCIA_INICIAL,mesAtual());await carregar();autoTxt=` Conferência automática: ${conferidas} vínculo(s), ${pendentes} pendente(s).`}catch{autoTxt=' A conferência automática falhou — clique em Reprocessar conferência.'}setProgressoBaixa({ativo:true,percentual:100,etapa:`Concluído · ${totalEncontradas} encontrada(s) · ${totalGravadas} gravada(s) · ${totalIgnoradas} ignorada(s)${totalCanceladas?` · ${totalCanceladas} cancelada(s)`:''}`});setMsg(totalGravadas||totalEncontradas?`Baixa concluída em ${ids.length} unidade(s): ${totalEncontradas} encontrada(s) e ${totalGravadas} gravada(s)${totalCanceladas?`, ${totalCanceladas} cancelada(s) expurgada(s)`:''}.${autoTxt}`:`Nada novo para baixar — todas as ${ids.length} unidade(s) já estão em dia com o gov.br (último NSU alcançado).${autoTxt}`)}
  }catch(e){setProgressoBaixa({ativo:true,percentual:0,etapa:'A baixa não pôde ser iniciada'});setMsg(e instanceof Error?e.message:'Falha ao baixar as notas.')}finally{setBaixando(false)}
 }
 async function reprocessarCanceladas(){if(reprocCancel)return;setReprocCancel(true);setMsg('Reprocessando cancelamentos do ADN (re-varredura da distribuição)…');try{const certificados=await listarCertificados();const ids=Array.from(new Set(certificados.map(c=>c.empresa_id).filter(id=>!empresaId||id===empresaId)));if(!ids.length)throw new Error(empresaId?'Esta unidade não possui certificado cadastrado.':'Nenhuma unidade possui certificado cadastrado.');const nomeEmpresa=(id:string)=>{const e=empresas.find(x=>x.id===id);return e?.apelido||e?.razao_social||'Unidade'};let totalMarcadas=0,totalDesv=0;for(const id of ids){let nsu=0;for(let rodada=1;rodada<=400;rodada++){const res=await fetch('/api/nfse/reprocessar-cancelamentos',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({empresa_id:id,nsu})});const j=await res.json().catch(()=>null);if(!res.ok||!j?.ok){if(j?.rateLimited){setMsg(`gov.br limitou ${nomeEmpresa(id)}. Aguardando 3s… (${totalMarcadas} cancelada(s) até aqui)`);await new Promise(r=>setTimeout(r,3000));continue}throw new Error(j?.erro||`Falha ao reprocessar ${nomeEmpresa(id)}.`)}totalMarcadas+=Number(j.marcadas||0);totalDesv+=Number(j.desvinculadas||0);setMsg(`Reprocessando ${nomeEmpresa(id)}… ${totalMarcadas} cancelada(s) marcada(s). NSU ${j.proximoNsu}.`);if(j.rateLimited){await new Promise(r=>setTimeout(r,3000))}if(!j.houveMais)break;nsu=Number(j.proximoNsu||0);await new Promise(r=>setTimeout(r,600))}}await carregar();setMsg(`Reprocessamento concluído: ${totalMarcadas} nota(s) cancelada(s) marcada(s)${totalDesv?`, ${totalDesv} desvinculada(s) da conferência`:''}.`)}catch(e){setMsg(e instanceof Error?e.message:'Falha no reprocessamento de cancelamentos.')}finally{setReprocCancel(false)}}
 async function importarArquivos(files:FileList|null){if(!files?.length)return;if(!empresaId){setMsg('Selecione uma empresa antes de importar os XMLs.');return}setImportando(true);setMsg('Lendo e validando os XMLs…');const formData=new FormData();formData.set('empresa_id',empresaId);Array.from(files).forEach(a=>formData.append('arquivos',a));try{const res=await fetch('/api/nfse/importar-xml',{method:'POST',body:formData});const j=await res.json().catch(()=>null);if(!res.ok){setMsg(j?.erro||`Falha HTTP ${res.status} ao importar.`);return}const auto=j.conferencia?` Conferência automática: ${j.conferencia.conferidas} vínculo(s), ${j.conferencia.divergencias} divergência(s).`:'';const canc=Number(j.canceladas||0)?`, ${j.canceladas} cancelada(s) ignorada(s)`:'';const resumo=`${j.importadas} importada(s), ${j.duplicadas} duplicada(s)${canc} e ${j.rejeitadas} rejeitada(s), em ${j.arquivos} XML(s).${auto}`;setMsg(j.erros?.length?`${resumo} ${j.erros.join(' | ')}`:resumo);await carregar()}catch(e){setMsg(e instanceof Error?e.message:'Falha ao importar XMLs.')}finally{setImportando(false)}}
 // Reconciliação (casamento automático seguro) de um intervalo de competências.
 async function reconciliarIntervalo(de:string,ate:string){let conferidas=0,pendentes=0,divergencias=0;for(const competencia of mesesEntre(de,ate)){const res=await fetch('/api/salao/conferencia',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({acao:'reconciliar',competencia,empresaId:empresaId||undefined})});const j=await res.json().catch(()=>null);if(!res.ok||!j?.ok)throw new Error(j?.erro||`Falha ao conferir ${competencia}.`);conferidas+=Number(j.conferidas||0);pendentes+=Number(j.pendentes||0);divergencias+=Number(j.divergencias||0)}return{conferidas,pendentes,divergencias}}
 async function reprocessarConferencia(){const de=compInicio||inicio,ate=compFim||fim;if(!de||!ate||de>ate){setMsg('Informe um período válido para a reconferência.');return}setReconferindo(true);setMsg('Reprocessando a conferência do período…');try{const{conferidas,pendentes}=await reconciliarIntervalo(de,ate);setMsg(`Reconferência concluída: ${conferidas} novo(s) vínculo(s) e ${pendentes} pendente(s). A automática só vincula correspondência única de unidade, CNPJ/CPF e valor exato.`);await carregar()}catch(e){setMsg(e instanceof Error?e.message:'Falha ao reprocessar conferência.')}finally{setReconferindo(false)}}
 function abrirEdicao(n:NotaRecebida){setSelecionada(n);setEditando(true);setForm({emitente_nome:n.emitente_nome||'',documento:n.documento||'',numero:n.numero||'',data_emissao:n.data_emissao||'',competencia:n.competenciaEfetiva||'',valor:String(n.valor||''),observacao:n.observacao||''})}
 async function salvar(){if(!selecionada)return;const r=await editarNota(selecionada.id,form,usuario);if(!r.ok){setMsg(r.erro||'Erro ao salvar.');return}setMsg('Nota atualizada e vínculos reavaliados.');setEditando(false);setSelecionada(null);carregar()}
 async function outro(n:NotaRecebida){if(acaoId)return;setAcaoId(n.id);setLinhas(v=>v.filter(x=>x.id!==n.id));const r=await classificarNota(n.id,'outro_servico',{categoria:'Outro serviço',observacao:'Classificação rápida pelo painel',usuario});if(!r.ok){setMsg(r.erro||'Erro ao classificar.');carregar();setAcaoId('');return}setMsg('Nota movida para Outros serviços em um clique.');setAcaoId('')}
 async function excecaoHistorica(n:NotaRecebida){if(acaoId)return;setAcaoId(n.id);const motivo=`Nota anterior a janeiro de ${new Date().getFullYear()} — exceção histórica`;const r=await setAnaliseManual('nota',n.id,true,motivo,usuario);if(!r.ok){setMsg(r.erro||'Erro ao marcar exceção.');setAcaoId('');return}setLinhas(v=>v.map(x=>x.id===n.id?{...x,analise_manual:true,analise_motivo:motivo}:x));setMsg('Nota marcada como exceção histórica.');setAcaoId('')}
 async function conferir(n:NotaRecebida){if(acaoId)return;setAcaoId(n.id);const proximo=!n.conferida;setLinhas(v=>v.map(x=>x.id===n.id?{...x,conferida:proximo}:x));const r=await setNotaConferida(n.id,proximo,usuario);if(!r.ok){setMsg(r.erro||'Erro ao atualizar conferência.');carregar();setAcaoId('');return}setMsg(proximo?'Nota marcada como conferida.':'Nota voltou para pendentes.');setAcaoId('')}
 async function excluir(n:NotaRecebida){if(acaoId)return;setAcaoId(n.id);setLinhas(v=>v.filter(x=>x.id!==n.id));const r=await excluirNota(n.id,'Exclusão rápida pelo painel',usuario);if(!r.ok){setMsg(r.erro||'Erro ao excluir.');carregar();setAcaoId('');return}setMsg('Nota removida. Você pode restaurá-la na auditoria.');setAcaoId('')}
 if(!SALAO_ENABLED)return null
 return <LayoutAdmin title="Salão — Notas baixadas"><div className="space-y-4">
  {msg&&<div className="rounded-lg border border-blue-200 bg-blue-50 px-4 py-3 text-sm text-blue-800">{msg}</div>}
  <section className="rounded-xl border border-slate-200 bg-white p-4 shadow-sm">
    <h2 className="mb-1 text-sm font-semibold text-slate-800">Baixar notas recebidas do gov.br</h2>
    <p className="mb-3 text-xs leading-relaxed text-slate-500">O gov.br entrega as notas em lotes, da mais antiga para a mais recente. A baixa continua automaticamente do último NSU salvo até terminar todo o histórico. Notas próprias, canceladas ou sem valor são ignoradas.</p>
    <div className="flex flex-wrap items-end gap-3">
      <div className="min-w-[240px] flex-1"><label className="label-field">Empresa</label><select className="input-field" value={empresaId} onChange={e=>setEmpresaId(e.target.value)} disabled={baixando}><option value="">Todas as empresas</option>{empresas.map(e=><option key={e.id} value={e.id}>{e.apelido||e.razao_social}</option>)}</select></div>
      {!baixando?<><button onClick={()=>baixar(false)} className="flex items-center gap-2 rounded-lg bg-emerald-600 px-4 py-2 font-medium text-white hover:bg-emerald-700"><svg className="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-8l-4-4m0 0L8 8m4-4v12"/></svg>Baixar notas ({empresaId?'1 empresa':'todas'})</button><button onClick={()=>baixar(true)} title="Reinicia o marcador NSU e baixa desde o começo" className="btn-secondary text-sm">Rebaixar do zero</button></>:<button onClick={()=>{pararBaixa.current=true;setProgressoBaixa(p=>({...p,etapa:'Parando após concluir a etapa atual…'}))}} className="rounded-lg bg-red-600 px-4 py-2 font-medium text-white hover:bg-red-700">Parar</button>}
    </div>
    {progressoBaixa.ativo&&<div className="mt-3" role="status" aria-live="polite"><div className="flex items-center gap-2 text-sm text-slate-700">{baixando&&<svg className="h-4 w-4 animate-spin text-emerald-600" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"/></svg>}<span>{progressoBaixa.etapa}</span></div><div className="mt-2 h-1.5 w-full overflow-hidden rounded-full bg-slate-100"><div className="h-full rounded-full bg-emerald-500 transition-all duration-500" style={{width:`${Math.max(baixando?6:0,progressoBaixa.percentual)}%`}}/></div></div>}
  </section>
  
  {syncResumo?.empresas?.length?(()=>{const amb=syncResumo.ambiente||'';const ehTeste=amb.includes('producaorestrita');const ambLabel=amb.includes('producaorestrita')?'Ambiente de TESTE (produção restrita)':amb.includes('adn.nfse.gov.br')?'Ambiente de produção':rotuloAmbiente(amb);return <section className="rounded-xl border bg-white p-3 shadow-sm"><div className="mb-3 flex flex-wrap items-center justify-between gap-2"><h2 className="text-sm font-semibold text-gray-800">{baixando?'Contagem ao vivo por unidade':'Resultado da última baixa por unidade'}</h2><div className="flex items-center gap-2">{ambLabel&&<span className={`rounded-full px-2 py-0.5 text-[11px] font-semibold ${ehTeste?'bg-amber-100 text-amber-800':'bg-slate-100 text-slate-600'}`} title="Ambiente do ADN configurado (SALON_ADN_AMBIENTE)">{ambLabel}</span>}<span className={`text-xs font-semibold ${baixando?'text-emerald-700':'text-gray-500'}`}>{baixando?'● Atualizando':'Finalizado'}</span></div></div>{ehTeste&&<div className="mb-3 rounded-lg border border-amber-200 bg-amber-50 px-3 py-2 text-[11px] text-amber-800">⚠ A baixa está apontada para o <b>ambiente de teste</b> do gov.br, que normalmente não tem as notas reais — por isso pode voltar <b>404 / nada encontrado</b>. Para produção, defina <code>SALON_ADN_AMBIENTE=producao</code> nas variáveis de ambiente e rebaixe.</div>}<div className="grid gap-3 md:grid-cols-2 xl:grid-cols-3">{syncResumo.empresas.map(e=>{const aguardando=e.status===-1;const ativo=baixando&&!!e.ativo;const falhou=!e.ok&&!aguardando&&(e.status>0||!!e.erro);const emDia=e.ok&&!e.houveMais&&(e.encontradas||0)===0&&e.status!==-1;const classe=aguardando?'border-slate-200 bg-slate-50':ativo?'border-blue-200 bg-blue-50':falhou?'border-red-200 bg-red-50':emDia?'border-slate-200 bg-white':'border-emerald-200 bg-emerald-50';const rotulo=aguardando?'Na fila':ativo?'Baixando…':falhou?'Falhou':emDia?'Em dia':'✓ Concluída';const rotuloCor=aguardando?'text-slate-500':ativo?'text-blue-700':falhou?'text-red-700':emDia?'text-slate-500':'text-emerald-700';const faltam=e.maxNsu&&e.ultimoNsu!=null?Math.max(0,e.maxNsu-e.ultimoNsu):0;const pct=e.maxNsu?Math.min(100,Math.round(((e.ultimoNsu||0)/e.maxNsu)*100)):(e.ok?100:0);return <div key={e.empresa_id} className={`rounded-xl border p-3 ${classe}`}><div className="flex items-center justify-between gap-2"><b className="truncate text-sm text-slate-900" title={e.empresaNome}>{e.empresaNome}</b><span className={`flex items-center gap-1 whitespace-nowrap text-xs font-semibold ${rotuloCor}`}>{ativo&&<svg className="h-3 w-3 animate-spin" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"/></svg>}{rotulo}</span></div>{(e.etapa||ativo)&&<p className={`mt-1 truncate text-[11px] ${ativo?'text-blue-700':'text-slate-500'}`} title={e.etapa}>{e.etapa||'Processando…'}</p>}<div className="mt-3 grid grid-cols-3 gap-2 text-center"><div><b className="block text-xl tabular-nums text-slate-900">{e.encontradas||0}</b><span className="text-[10px] uppercase text-slate-500">Encontradas</span></div><div><b className="block text-xl tabular-nums text-emerald-700">{e.gravadas||0}</b><span className="text-[10px] uppercase text-slate-500">Gravadas</span></div><div><b className="block text-xl tabular-nums text-slate-600">{e.ignoradas||0}</b><span className="text-[10px] uppercase text-slate-500">Ignoradas</span></div></div>{!!e.maxNsu&&<div className="mt-2"><div className="h-1.5 w-full overflow-hidden rounded-full bg-slate-100"><div className={`h-full rounded-full ${faltam?'bg-blue-500':'bg-emerald-500'} transition-all`} style={{width:`${pct}%`}}/></div></div>}<div className="mt-2 flex flex-wrap justify-between gap-x-2 border-t pt-2 text-[10px] text-slate-500"><span title="Código HTTP da última resposta do gov.br">{emDia?'Em dia (nada novo)':falhou?`HTTP ${e.status>0?e.status:'erro'}`:`HTTP ${e.status>0?e.status:'—'}`}</span>{Number(e.canceladas||0)>0&&<span className="font-semibold text-amber-700">{e.canceladas} cancelada(s)</span>}<span title="NSU salvo / maior NSU disponível no gov.br">NSU {e.ultimoNsu??'—'}{e.maxNsu?` / ${e.maxNsu}`:''}</span>{faltam>0&&<span className="w-full font-semibold text-blue-700">Faltam ~{faltam} documento(s) — clique em Baixar de novo para continuar</span>}</div>{(e.ignoradas||0)>0&&<p className="mt-1 text-[10px] text-slate-400" title="Motivo de cada nota descartada">Ignoradas: {e.ignoradasProprias||0} da própria empresa · {e.ignoradasSemValor||0} sem valor · {e.ignoradasCanceladas||0} cancelada(s)</p>}{e.dica&&<p className="mt-2 rounded-lg bg-amber-50 px-2 py-1.5 text-[11px] leading-snug text-amber-800">💡 {e.dica}</p>}{e.erro&&<p className="mt-2 text-xs text-red-700">{e.erro}</p>}{e.amostra&&(e.gravadas||0)===0&&<details className="mt-2 text-[10px] text-slate-500"><summary className="cursor-pointer hover:text-slate-700">Ver resposta do gov.br</summary><pre className="mt-1 max-h-32 overflow-auto whitespace-pre-wrap break-all rounded bg-slate-50 p-2">{e.amostra}</pre></details>}<button onClick={()=>baixar(true,e.empresa_id)} disabled={baixando} title="Re-varre esta unidade do início (idempotente — não duplica)" className="mt-2 w-full rounded-lg border border-slate-300 bg-white px-2 py-1 text-[11px] font-semibold text-slate-700 hover:bg-slate-50 disabled:opacity-50">🔄 Re-verificar esta unidade</button></div>})}</div></section>})():null}
  <section className="rounded-xl bg-slate-900 p-4 text-white"><div className="flex flex-wrap items-end gap-3"><div className="mr-auto"><h1 className="text-lg font-semibold">Notas baixadas</h1><p className="text-xs text-slate-300">Consulte, classifique e corrija as NFS-e recebidas.</p></div><div><label className="block text-xs text-slate-300">Emissão inicial</label><input type="month" min={SALAO_COMPETENCIA_INICIAL} className="mt-1 rounded-lg border border-slate-600 bg-slate-800 px-3 py-2 text-sm" value={inicio} onChange={e=>setInicio(e.target.value)}/></div><div><label className="block text-xs text-slate-300">Emissão final</label><input type="month" className="mt-1 rounded-lg border border-slate-600 bg-slate-800 px-3 py-2 text-sm" value={fim} min={inicio>SALAO_COMPETENCIA_INICIAL?inicio:SALAO_COMPETENCIA_INICIAL} onChange={e=>setFim(e.target.value<inicio?inicio:e.target.value)}/></div><div><label className="block text-xs text-slate-300">Competência inicial</label><input type="month" min={SALAO_COMPETENCIA_INICIAL} className="mt-1 rounded-lg border border-slate-600 bg-slate-800 px-3 py-2 text-sm" value={compInicio} onChange={e=>setCompInicio(e.target.value)}/></div><div><label className="block text-xs text-slate-300">Competência final</label><input type="month" className="mt-1 rounded-lg border border-slate-600 bg-slate-800 px-3 py-2 text-sm" value={compFim} min={compInicio>SALAO_COMPETENCIA_INICIAL?compInicio:SALAO_COMPETENCIA_INICIAL} onChange={e=>setCompFim(e.target.value)}/></div><div className="min-w-48"><label className="block text-xs text-slate-300">Empresa</label><select className="mt-1 w-full rounded-lg border border-slate-600 bg-slate-800 px-3 py-2 text-sm" value={empresaId} onChange={e=>setEmpresaId(e.target.value)}><option value="">Todas</option>{empresas.map(e=><option key={e.id} value={e.id}>{e.apelido||e.razao_social}</option>)}</select></div><button onClick={()=>{setInicio(SALAO_COMPETENCIA_INICIAL);setFim(mesAtual());setCompInicio(SALAO_COMPETENCIA_INICIAL);setCompFim(mesAtual());setEmpresaId('');setBusca('');setBuscaAplicada('');setFiltro('pendentes')}} className="rounded-lg border border-slate-600 px-3 py-2 text-sm text-slate-200 hover:bg-slate-800">Limpar filtros</button><input ref={seletorXml} type="file" accept=".zip,.xml,application/zip,text/xml,application/xml" multiple className="hidden" onChange={e=>{importarArquivos(e.target.files);e.target.value=''}}/><button onClick={()=>{if(!empresaId){setMsg('Selecione uma empresa antes de importar.');return}seletorXml.current?.click()}} disabled={importando} className="rounded-lg bg-emerald-600 px-4 py-2 text-sm font-medium hover:bg-emerald-500 disabled:opacity-50">{importando?'Importando XMLs…':'Importar ZIP/XML'}</button><button onClick={reprocessarConferencia} disabled={reconferindo} className="rounded-lg bg-violet-600 px-4 py-2 text-sm font-medium hover:bg-violet-500 disabled:opacity-50">{reconferindo?'Reconferindo…':'Reprocessar conferência'}</button><button onClick={reprocessarCanceladas} disabled={reprocCancel} title="Re-varre o ADN e marca as notas canceladas na origem (eventos de cancelamento)" className="rounded-lg bg-red-600 px-4 py-2 text-sm font-medium hover:bg-red-500 disabled:opacity-50">{reprocCancel?'Reprocessando…':'Reprocessar canceladas (ADN)'}</button></div></section>
  <section className="grid grid-cols-2 gap-2 md:grid-cols-6">{([{k:'todas',v:resumo.total,l:'Todas'},{k:'pendentes',v:linhas.filter(n=>!n.conferida&&!n.analise_manual).length,l:'Pendentes'},{k:'conferidas',v:resumo.conferidas,l:'Conferidas'},{k:'divergentes',v:resumo.divergentes,l:'Divergentes'},{k:'analise',v:linhas.filter(n=>n.analise_manual).length,l:'Análise'},{k:'canceladas',v:canceladas.size,l:'Canceladas'}] as {k:Filtro;v:number;l:string}[]).map(x=><button key={x.k} onClick={()=>setFiltro(x.k)} className={`rounded-xl border bg-white p-3 text-left shadow-sm ${filtro===x.k?'ring-2 ring-blue-500':''} ${x.k==='canceladas'&&x.v>0?'border-red-200':''}`}><b className={`block text-xl ${x.k==='canceladas'&&x.v>0?'text-red-700':'text-gray-900'}`}>{x.v}</b><span className="text-xs text-gray-500">{x.l}</span></button>)}</section>
  <section className="rounded-xl border bg-white shadow-sm"><div className="flex flex-wrap items-center gap-2 border-b p-3"><input type="search" className="input-field flex-1" placeholder="Buscar profissional por partes do nome, CNPJ/CPF, unidade ou número da nota" value={busca} onChange={e=>setBusca(e.target.value)}/><span className="text-xs text-gray-500">{visiveis.length} nota(s) · {formatarMoeda(visiveis.reduce((s,n)=>s+(n.valor||0),0))}</span></div>{loading?<div className="py-14 text-center text-sm text-gray-400">Carregando...</div>:visiveis.length===0?<div className="py-14 text-center"><p className="text-sm font-medium text-gray-600">Nenhuma nota encontrada.</p><p className="mt-1 text-xs text-gray-400">Limpe ou ajuste os filtros para ampliar a busca.</p></div>:<div className="overflow-x-auto"><table className="w-full"><thead><tr className="bg-gray-50"><th className="table-header">Emitente</th><th className="table-header">CPF/CNPJ</th><th className="table-header">Nota</th><th className="table-header">Emissão</th><th className="table-header">Competência</th><th className="table-header text-right">Valor</th><th className="table-header">Situação</th><th className="table-header text-right">Ações rápidas</th></tr></thead><tbody className="divide-y">{visiveis.map(n=><tr key={n.id} className="hover:bg-gray-50"><td className="table-cell font-medium">{n.emitente_nome||'—'}</td><td className="table-cell text-xs">{fmtDoc(n.documento)}</td><td className="table-cell">{n.numero||'—'}</td><td className="table-cell">{fmtData(n.data_emissao)}</td><td className="table-cell">{n.competenciaEfetiva?MESES[Number(n.competenciaEfetiva.slice(5,7))-1]+'/'+n.competenciaEfetiva.slice(0,4):'—'}</td><td className="table-cell text-right">{formatarMoeda(n.valor||0)}</td><td className="table-cell"><div className="flex flex-col items-start gap-1">{canceladas.has(n.id)&&<span className="rounded-full bg-red-100 px-2 py-1 text-xs font-semibold text-red-700">⛔ Cancelada</span>}<span className={`rounded-full px-2 py-1 text-xs ${n.conferida?'bg-green-100 text-green-700':'bg-amber-100 text-amber-700'}`}>{n.conferida?'✓ Conferida':n.analise_manual?'Análise':'Pendente'}</span></div></td><td className="table-cell text-right whitespace-nowrap"><button onClick={()=>conferir(n)} className={`mr-2 rounded-md px-2 py-1 text-xs font-semibold ${n.conferida?'bg-green-100 text-green-700':'bg-slate-100 text-slate-700'}`}>{n.conferida?'✓ Conferida':'Conferir'}</button><span className="mr-2 inline-flex align-middle"><NotaPreviewDialog compacto nota={{numero:n.numero,emitente:n.emitente_nome,documento:n.documento,valor:n.valor,emissao:n.data_emissao,competencia:n.competenciaEfetiva,competenciaOficial:false,situacao:n.conferida?'Conferida':n.analise_manual?'Em análise':'Pendente',observacao:n.observacao,unidade:n.empresaNome,xmlOriginal:n.xml_original,xmlNome:n.xml_nome}}/></span><button onClick={()=>abrirEdicao(n)} className="mr-2 text-xs text-blue-700">Editar</button><button onClick={()=>outro(n)} className="mr-2 text-xs text-amber-700">Outro serviço</button>{notaHistorica(n)&&<button onClick={()=>excecaoHistorica(n)} className="mr-2 text-[11px] text-slate-400 hover:text-slate-700">Exceção</button>}<button onClick={()=>excluir(n)} className="text-xs text-red-600">Excluir</button></td></tr>)}</tbody></table></div>}</section>
  {editando&&selecionada&&<section className="rounded-xl border border-blue-200 bg-white p-4 shadow-sm"><div className="mb-3 flex justify-between"><div><h2 className="font-semibold">Editar nota {selecionada.numero||''}</h2><p className="text-xs text-gray-500">A alteração será registrada no histórico e poderá desfazer vínculo incompatível.</p></div><button onClick={()=>setEditando(false)}>×</button></div><div className="grid gap-3 md:grid-cols-4"><div><label className="label-field">Emitente</label><input className="input-field" value={form.emitente_nome} onChange={e=>setForm({...form,emitente_nome:e.target.value})}/></div><div><label className="label-field">CPF/CNPJ</label><input className="input-field" value={form.documento} onChange={e=>setForm({...form,documento:e.target.value})}/></div><div><label className="label-field">Data</label><input type="date" className="input-field" value={form.data_emissao} onChange={e=>setForm({...form,data_emissao:e.target.value})}/></div><div><label className="label-field">Competência</label><input type="month" className="input-field" value={form.competencia} onChange={e=>setForm({...form,competencia:e.target.value})}/></div><div><label className="label-field">Número</label><input className="input-field" value={form.numero} onChange={e=>setForm({...form,numero:e.target.value})}/></div><div><label className="label-field">Valor</label><input className="input-field" value={form.valor} onChange={e=>setForm({...form,valor:e.target.value})}/></div><div className="md:col-span-2"><label className="label-field">Observação</label><input className="input-field" value={form.observacao} onChange={e=>setForm({...form,observacao:e.target.value})}/></div></div><div className="mt-4 flex justify-end gap-2"><button className="btn-secondary" onClick={()=>setEditando(false)}>Cancelar</button><button className="btn-primary" onClick={salvar}>Salvar alterações</button></div></section>}
 </div></LayoutAdmin>
}
